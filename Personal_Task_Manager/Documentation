📄 ТЗ — Personal Task Manager (Консольное приложение)

1. Описание

Программа — это консольный менеджер задач, позволяющий пользователю:
	•	Добавлять задачи.
	•	Просматривать их с фильтрацией и сортировкой.
	•	Редактировать или удалять задачи.
	•	Отмечать задачи как выполненные.
	•	Хранить все данные в локальном JSON-файле.

Приложение должно запускаться без ошибок и сохранять данные между сессиями.

⸻

2. Требования к функционалу

2.1 Добавление задачи
	•	Поля задачи:
	•	Id (уникальный, GUID или автоинкремент).
	•	Title (обязательное).
	•	Description (необязательное).
	•	Priority (Enum: Low, Medium, High).
	•	DueDate (дата дедлайна).
	•	IsCompleted (bool, по умолчанию false).
	•	Если пользователь ввёл некорректную дату или пустое название — выдать сообщение об ошибке и запросить ввод заново.

2.2 Просмотр задач
	•	Показать список всех задач в формате:
    -[ ] (High) 1. Сделать отчёт — дедлайн: 20.08.2025
    -[X] (Low) 2. Купить молоко — дедлайн: 15.08.2025

    	Возможность сортировки:
	•	По приоритету.
	•	По дедлайну.
	•	Возможность фильтрации:
	•	Только невыполненные.
	•	Только выполненные.

2.3 Поиск задач
	•	Поиск по названию или описанию (частичное совпадение, без учёта регистра).

2.4 Редактирование задачи
	•	Возможность изменить название, описание, приоритет и дедлайн.

2.5 Отметка задачи как выполненной
	•	Изменяет IsCompleted на true.

2.6 Удаление задачи
	•	Удаляет по ID.

⸻

3. Требования к данным
	•	Все задачи хранятся в файле tasks.json в корневой папке проекта.
	•	При запуске приложение загружает задачи из файла.
	•	При изменении данных — файл автоматически перезаписывается.
	•	Если файла нет — создаётся пустой.

⸻

4. Требования к коду
	•	Использовать:
	•	ООП: отдельный класс TaskItem и класс TaskManager.
	•	Enum для приоритета.
	•	**List`.
	•	LINQ для фильтрации и сортировки.
	•	System.Text.Json для сериализации/десериализации.
	•	try/catch для обработки ошибок.
	•	Разделить проект на папки:

    /Models — классы данных
    /Services — бизнес-логика   
    Program.cs — точка входа

    5. Критерии готовности

    ✅ Программа запускается без ошибок.
    ✅ После перезапуска сохраняет задачи.
    ✅ Поддерживает все указанные операции.
    ✅ Корректно обрабатывает некорректный ввод.
    ✅ Код структурирован и легко читается.